% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lab_funcs.R
\name{calc_data_ea}
\alias{calc_data_ea}
\title{Calculate per-mL density values from EA data}
\usage{
calc_data_ea(
  df,
  unit_col = "Organisms per mL",
  cell_col = "Cells per Unit",
  calc_cols = c("Units", "Cells", "Biovolume")
)
}
\arguments{
\item{df}{An EA dataframe containing raw count columns}

\item{unit_col}{Name of the column containing organisms per mL
(default: \code{'Organisms per mL'})}

\item{cell_col}{Name of the column containing cells per unit
(default: \code{'Cells per Unit'})}

\item{calc_cols}{Character vector specifying which density columns to
calculate. One or more of \code{'Units'}, \code{'Cells'}, \code{'Biovolume'}
(default: all three)}
}
\value{
Dataframe with one or more new density columns added:
\code{Units_per_mL}, \code{Cells_per_mL}, \code{Biovolume_per_mL}
}
\description{
Computes standardized density columns (\code{Units_per_mL}, \code{Cells_per_mL},
and/or \code{Biovolume_per_mL}) from EA-formatted data.
\code{Units_per_mL} is taken directly from the unit column.
Cells are derived by multiplying units by cells-per-unit.
Biovolume requires a factor column and is calculated by averaging all columns
matching \code{"Biovolume"} and multiplying by factor and cell count.
}
