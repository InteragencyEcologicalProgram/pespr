% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_funcs.R
\name{export_primer}
\alias{export_primer}
\title{Export community data in PRIMER format}
\usage{
export_primer(
  df_meta,
  df_com,
  title = "Environmental variables",
  meta_as_factors = TRUE
)
}
\arguments{
\item{df_meta}{A dataframe of sample metadata; one row per sample}

\item{df_com}{A community matrix dataframe or matrix; one row per sample,
one column per taxon/variable. Must have the same number of rows as
\code{df_meta}.}

\item{title}{Character string for the merged title cell
(default: \code{'Environmental variables'})}

\item{meta_as_factors}{Logical; if \code{TRUE}, metadata columns are appended
below the community data as factor rows (default: \code{TRUE})}
}
\value{
An \code{openxlsx} workbook object. Use \code{openxlsx::saveWorkbook()} to
write it to disk.
}
\description{
Formats a community matrix and optional metadata into the layout expected by
PRIMER-e, then writes it to an Excel workbook. The output has the title in
the first row (merged across all columns), sample IDs (\code{R1}, \code{R2}, ...) in
the second row, community variables as rows below, and metadata factors
appended at the bottom after a blank row.
}
