% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_funcs.R
\name{convert_to_sf}
\alias{convert_to_sf}
\title{Assign Delta regions to station coordinates}
\usage{
convert_to_sf(df, sf_delta = NULL)
}
\arguments{
\item{df}{A dataframe containing \code{Latitude} and \code{Longitude} columns}

\item{sf_delta}{Optional \code{sf} object of Delta subregion boundaries. If
\code{NULL}, the package's built-in \code{delta_geom} dataset is used.}
}
\value{
An \code{sf} object with \code{Region} and \code{SubRegion} columns added and rows
outside the Delta removed
}
\description{
Converts a dataframe with \code{Latitude} and \code{Longitude} columns to a spatial
object, performs a spatial overlay against the Delta subregion geometry
(\code{delta_geom}), and returns an \code{sf} object with \code{Region} and \code{SubRegion}
columns added. Rows whose coordinates fall outside all regions are dropped.
}
