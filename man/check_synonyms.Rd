% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_funcs.R
\name{check_synonyms}
\alias{check_synonyms}
\title{Check synonym reference table for multi-generational chains and flag missing synonym entries}
\usage{
check_synonyms(df)
}
\arguments{
\item{df}{input dataframe containing columns \code{Taxon} through \code{Species}}
}
\value{
A dataframe combining:
\itemize{
\item a summary table of multi-generational taxa from the synonym reference
\item the \code{Taxon} through \code{Species} rows from \code{df} where
\code{Taxon} is missing (\code{NA}) or equals \code{Unknown}
}
The returned dataframe includes a \code{Type} column with values \code{multigen}
and \verb{missing syn} to distinguish record types.
}
\description{
Reads a synonym/classification reference table and performs two checks:
\itemize{
\item Identifies multi-generational synonym chains in the reference table,
where a taxon that is itself updated (has \code{CurrentTaxon != 'None'}) also
appears as a \code{CurrentTaxon} for some other record
\item Identifies taxa in the input dataframe that are missing synonym coverage,
defined here as rows where \code{Taxon} is \code{NA} or equals \code{'Unknown'}
}

The function prints a message that includes any multi-generational warning text
and, if missing synonym taxa are present, lists the unique affected taxa.
}
