% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_funcs.R
\name{check_distinct}
\alias{check_distinct}
\title{Check for non-distinct rows under different key definitions}
\usage{
check_distinct(
  df,
  return_df = FALSE,
  coerce = TRUE,
  type = c("full", "key_measure_cols", "key_cols"),
  key_cols = c("Station", "Date"),
  taxa_cols = c("Taxon"),
  measurement_cols = c("Biovolume_per_mL", "Cells_per_mL", "Units_per_mL")
)
}
\arguments{
\item{df}{input dataframe to check}

\item{return_df}{logical; if \code{TRUE}, returns the non-distinct rows as a dataframe
instead of returning the original dataframe with a \code{log} attribute}

\item{coerce}{logical; if \code{TRUE}, coerces factor columns to character and rounds
numeric columns to 6 decimals prior to distinctness checks}

\item{type}{character; definition of distinctness to check, one of
\code{'full'}, \code{'key_measure_cols'}, or \code{'key_cols'}}

\item{key_cols}{character vector of key columns used to identify samples
(default \code{c('Station','Date')})}

\item{taxa_cols}{character vector of taxa identifier columns (default \code{c('Taxon')})}

\item{measurement_cols}{character vector of measurement columns used when
\code{type = 'key_measure_cols'} (default \code{c('Biovolume_per_mL','Cells_per_mL','Units_per_mL')})}
}
\value{
If no non-distinct rows are found, returns \code{invisible(df)} and stores \code{NULL} in the
relevant log entry.

If non-distinct rows are found:
\itemize{
\item when \code{return_df = TRUE}, returns a dataframe of non-distinct rows
\item otherwise returns \code{df} with \code{attr(df, 'log')} updated
}

The \code{log} attribute uses one of the following names depending on \code{type}:
\itemize{
\item \code{nondistinct_allrows} for \code{type = 'full'}
\item \code{nondistinct_keymeasurerows} for \code{type = 'key_measure_cols'}
\item \code{nondistinct_keyrows} for \code{type = 'key_cols'}
}
}
\description{
Checks whether rows in a dataframe are distinct under one of three definitions:
the full row (\code{type = 'full'}), the combination of key + taxa + measurement columns
(\code{type = 'key_measure_cols'}), or the combination of key + taxa columns (\code{type = 'key_cols'}).

If non-distinct rows are found, they are collected and either returned directly
(\code{return_df = TRUE}) or attached to the input dataframe via the \code{log} attribute.
}
