% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_funcs.R
\name{create_mcom}
\alias{create_mcom}
\title{Build a community matrix from long-format data}
\usage{
create_mcom(
  df,
  group_var,
  measure_var,
  taxa_var = "Taxon",
  factor_var = NULL,
  avg_var = NULL,
  compute_dist = FALSE,
  distance = NULL
)
}
\arguments{
\item{df}{A long-format dataframe}

\item{group_var}{Character vector of column names defining each sample event
(each unique combination becomes one row in the matrix)}

\item{measure_var}{Name of the numeric column to aggregate (e.g. \code{'Cells_per_mL'})}

\item{taxa_var}{Name of the taxon column to pivot to wide format
(default: \code{'Taxon'})}

\item{factor_var}{Optional character vector of additional metadata columns
to retain (e.g. grouping variables for coloring plots)}

\item{avg_var}{Optional character vector of columns to use for effort
correction; totals are divided by the number of distinct \code{avg_var} events
within each \code{group_var} combination}

\item{compute_dist}{Logical; if \code{TRUE}, computes a dissimilarity matrix
using \code{vegdist()} and attaches it to the output as \code{m_dist}
(default: \code{FALSE})}

\item{distance}{Distance metric to pass to \code{vegdist()} (e.g. \code{'bray'});
only used when \code{compute_dist = TRUE}}
}
\value{
A named list with elements:
\itemize{
\item \code{m_all} — combined metadata and community matrix as a dataframe
\item \code{m_com} — numeric community matrix
\item \code{m_meta} — metadata columns only
\item \code{meta_vars} — list of the variable name arguments
\item \code{m_dist} — dissimilarity matrix (only present if \code{compute_dist = TRUE})
}
}
\description{
Aggregates a long-format dataframe into a wide community matrix suitable
for multivariate analysis. Optionally applies an effort correction by
dividing totals by the number of unique sampling events in \code{avg_var}.
Returns both the full combined dataframe and the raw community matrix.
}
