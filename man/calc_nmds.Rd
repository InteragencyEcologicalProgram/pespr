% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_funcs.R
\name{calc_nmds}
\alias{calc_nmds}
\title{Run NMDS on long-format community data}
\usage{
calc_nmds(
  df,
  group_var,
  measure_var,
  taxa_var = "Taxon",
  factor_var = NULL,
  avg_var = NULL,
  distance = "bray",
  show_legend = TRUE,
  color_palette = NULL,
  try = 20,
  trymax = 20
)
}
\arguments{
\item{df}{A long-format dataframe}

\item{group_var}{Character vector of column names defining each sample event
(each unique combination becomes one point in the ordination)}

\item{measure_var}{Name of the numeric column to aggregate (e.g. \code{'Cells_per_mL'})}

\item{taxa_var}{Name of the taxon column (default: \code{'Taxon'})}

\item{factor_var}{Optional character vector of metadata columns to retain
alongside NMDS scores (e.g. for coloring plots)}

\item{avg_var}{Optional character vector of columns for effort correction}

\item{distance}{Distance metric to use (default: \code{'bray'})}

\item{show_legend}{Logical; currently unused, reserved for future use}

\item{color_palette}{Currently unused, reserved for future use}

\item{try}{Number of random starts for NMDS (default: \code{20})}

\item{trymax}{Maximum number of random starts (default: \code{20})}
}
\value{
A named list with elements:
\itemize{
\item \code{df_nmds} — dataframe of NMDS1/NMDS2 site scores joined with metadata
\item \code{m_com} — numeric community matrix used in the ordination
\item \code{m_meta} — metadata columns only
\item \code{meta_vars} — list of the variable name arguments
}
}
\description{
Aggregates long-format data into a community matrix and runs non-metric
multidimensional scaling (NMDS) via \code{vegan::metaMDS()}. Optionally applies
effort correction if \code{avg_var} is provided. Returns NMDS site scores joined
with sample metadata.
}
